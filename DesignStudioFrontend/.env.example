# Copy this file to .env and fill in values for production or customized dev runs.

# When true, bypasses OAuth and uses MSW mock API. If not set, it defaults to true in non-production environments.
# REACT_APP_BYPASS_AUTH=true

# Backend base URL for API requests when not bypassing auth (e.g., https://api.example.com)
# REACT_APP_API_BASE_URL=https://api.example.com

# OAuth settings (required if REACT_APP_BYPASS_AUTH is false)
# REACT_APP_OAUTH_AUTHORIZATION_URL=https://identity.example.com/oauth2/authorize
# REACT_APP_OAUTH_CLIENT_ID=your-client-id
# REACT_APP_OAUTH_REDIRECT_URI=https://your.app/callback
# REACT_APP_OAUTH_SCOPE=openid profile email

# Site URL used by some flows (optional)
# REACT_APP_SITE_URL=https://your.app
